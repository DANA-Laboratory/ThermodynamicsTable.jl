language: julia
julia:
  - 0.5
  - nightly
os :
  - linux
  - osx
notifications:
  email: false
# uncomment the following lines to override the default test script
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("ThermodynamicsTable"); Pkg.test("ThermodynamicsTable"; coverage=true)'
